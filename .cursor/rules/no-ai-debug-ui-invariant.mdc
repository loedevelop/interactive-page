---
description: 嚴禁 AI 在 UI 畫面上注入任何除錯文字、自我提示或非正式的無意義文字
globs: *.html, *.js, *.jsx, *.ts, *.tsx, *.vue, *.svelte
---
# UI Clean Code Invariant (嚴禁 UI 畫面污染原則)

## 核心原則 (Core Principle)
你正在撰寫的是 Production-Ready 的應用程式。**絕對禁止**在應用程式的 UI 視圖 (DOM, HTML, 畫面渲染層) 中注入任何「給 AI 自己看」的文字、除錯標記或無意義的 Placeholder。

## 嚴格禁止的行為 (Strictly Forbidden)
1. **禁止 UI 塗鴉**：絕不可在畫面上印出自我提示或除錯訊息 (例如：`<div>這裡是有資料的</div>`, `<span>AI debug step 1</span>`, `<!-- 給我看的備註 -->`)。
2. **禁止狀態裸奔**：絕不可將變數、物件或 JSON 直接渲染在畫面上來「幫助你理解當前狀態」。
3. **禁止發明廢話**：除了真實的業務文案與規格要求的 UI 文字，禁止在畫面上塞入任何沒有意義的佔位字元。

## 合法的思考與除錯管道 (Allowed Channels)
如果你在撰寫複雜邏輯時需要記錄狀態或自我提醒，只能透過以下兩種方式：
1. **開發者註解**：使用標準程式碼註解 (如 `//` 或 `/* */`)，且內容必須是專業的工程說明，而非你的個人碎碎念。
2. **Console 輸出**：在非正式環境下，允許暫時使用 `console.log()` 或 `console.debug()` 將狀態輸出到終端機，但嚴禁干擾使用者可見的 UI。