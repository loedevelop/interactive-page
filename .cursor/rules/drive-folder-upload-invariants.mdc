---
description: Drive 資料夾層級與學生上傳鐵律（禁止再把繳交誤導進 01_Class_Resources；多檔上傳中途失敗不可整批丟；取代特定已上傳檔）
globs: gas/Code.gs,020_js_core/api.js,110_teacher_core/api-gas-service.js,120_student_core/feature-student-timeline.js,120_student_core/ui-student-timeline-templates.js
alwaysApply: false
---

# Drive／學生上傳鐵律

曾發生「本來對的被改錯、再改回去」：GAS `upload_file` 在未傳 `subFolderName` 時誤呼叫 `resolveClassResourcesFolder`，在學生夾下長出錯誤的 `01_Class_Resources`，繳交檔消失在老師視線外。

## 正確目錄（不可改亂）

```
班級根/
  00_Class_Materials/     ← 教材 master
  01_Class_Resources/     ← 僅班級層資源（老師上傳教材用）
  02_Students/
    姓名_短ID/
      01_Submissions/     ← 學生繳交唯一目標（enrollment.drive_folder_id）
```

- **禁止**在 `02_Students/姓名_短ID/` 下建立 `01_Class_Resources`
- **禁止**把學生 `upload_file` 預設導向 `01_Class_Resources`

## GAS `upload_file`（gas/Code.gs）

```javascript
// ✅ 正確：只有明確指定才進班級資源夾；空 subFolderName = 直接寫入 folderId
if (subFolderName === '01_Class_Resources' || subFolderName === '01_Materials') {
  folder = resolveClassResourcesFolder(folder, true);
} else if (subFolderName) {
  folder = getOrCreateSubFolder(folder, subFolderName);
}
// else: 不動 folder（學生 ApiService.uploadToGAS 走這條）

// ❌ 禁止：!subFolderName 時也 resolveClassResourcesFolder
```

| 呼叫端 | subFolderName | 寫入位置 |
|--------|---------------|----------|
| 學生 `ApiService.uploadToGAS` | 不傳／空 | `folderId`（= 01_Submissions） |
| 老師 `GasService.uploadStudentLocalFile` | 明確 `'01_Class_Resources'` | 班級資源夾 |

## 產品行為（勿再搞混）

- 教材發布：**本機** Excel → `publish_local` → `.meta.json` / `.script.txt`。禁止要求老師上傳 Excel 給 GAS、禁止強制轉 Google Sheets。
- `drive` = 上傳到學生專屬夾；`audio_record` = 錄音艙。差異只在音檔怎麼產出。
- **禁止**用標題含 Recording 就把 `drive` 改成錄音 UI。
- 有 `original_script` 才送 AI；無文稿仍可上傳，略過 AI，不可擋上傳。
- **禁止**無文稿仍呼叫 `submit_audio_task_atomic`／讓 `process-audio-ai` 丟 `Fatal: original_script is missing`（應 skip → status `submitted`）。
- **學生端音檔播放**：`<audio src>` 必須用 `ApiService.getAudioStreamUrl`（Supabase `stream-audio`）。**禁止**把 GAS Web App URL 當 audio src（會 redirect → `0:00/0:00`）。曾修好又被改壞過，勿再翻。
- **已繳交確認**：有檔時開「該檔」的 Drive file 連結／內嵌播放／圖片預覽，不要只開 `01_Submissions` 資料夾。

## 多檔上傳（非 AI 批改）中途失敗鐵律

> 2026-08-01：學生反應「6 個檔案不收，3 個可以」。根因：`handleFileSelect` 的多音檔迴圈是單一 `for` 迴圈，
> 其中一檔 `uploadToGAS` 丟例外（重試 1 次後仍失敗）就整個 `throw`，導致第 4~6 檔完全沒被嘗試；
> 已成功的第 1~3 檔卻沒有任何「部分成功」訊息，學生只看到失敗、以為全部沒收到。
> 此路徑**不是** `grading_units` 頁數對應（那是 AI 批改專用的 `uploadAudioFilesForGrading`，見 `ai-grading-pipeline-invariants.mdc`）。

### 症狀
- 學生選 N 個檔案（無 AI 批改），只有選檔數量較少（如 3 個）時會成功，選多（如 6 個）就在某檔之後全部消失
- 畫面只顯示紅色失敗，看不出「其實前面幾檔已經上傳成功」

### 不變量
```
多檔迴圈中，單一檔案失敗 ≠ 整批失敗：必須逐檔 try/catch，繼續嘗試剩餘檔案
成功的檔案必須回報進度（updateProgress），不可因後面檔案失敗而整批不呼叫
task_completions.raw_data.submitted_files 在同一 task 重複呼叫 updateProgress 時，
  必須與既有已存檔案「合併去重」，不可覆寫掉前一批已成功上傳的紀錄
失敗訊息必須列出「第幾個／哪個檔名」失敗，讓學生知道只需重傳失敗的部分
```

### 禁止
```javascript
// ❌ 單一 for 迴圈，任何一檔失敗直接 throw，中斷整批
for (let i = 0; i < filesArray.length; i++) {
  const result = await window.ApiService.uploadToGAS(...); // 失敗就整個函式 throw
}

// ❌ updateProgress 用「這次呼叫」的 fileIds 直接覆寫 raw_data.submitted_files
rawData = { drive_file_ids: idList, submitted_files: normalizedFiles }; // 沒有跟 prevCompletion 合併
```

### 正確做法
```javascript
// ✅ 逐檔 try/catch，收集 failedFiles，成功的照樣 updateProgress
try { ... uploadToGAS(...) ... uploadedFileIds.push(...) }
catch (fileErr) { failedFiles.push({ index: i+1, name: file.name, message: fileErr.message }); }
// 迴圈結束後：有成功就先回報進度；有失敗就明確列出「第幾個檔名」失敗

// ✅ updateProgress 內合併既有 submitted_files（依 id 去重）再存回 raw_data
const existingFiles = prevCompletion?.raw_data?.submitted_files || [];
const merged = dedupById(existingFiles, normalizedFiles);
```

參考實作：`120_student_core/feature-student-timeline.js` 的 `handleFileSelect`（多音檔迴圈）與 `updateProgress`。

## 取代特定已上傳檔（2026-08-09）

> 需求：分批上傳會累加（正常），但學生若要「換掉某一個特定檔案」原本無法做到——多檔只會加不會刪。
> 已繳交清單「🔁 取代」按鈕＋`handleReplaceFile` 解決；**兩種情境共用同一套 GAS/前端機制，沒有新開 RPC**。

### 資料流

```
buildSubmittedFilesHtml（ui-student-timeline-templates.js）
  → 每筆檔案旁「🔁 取代」+ 隱藏 <input type="file" data-old-file-id data-unit-key data-stem data-page data-label data-script-b64>
  → onchange = handleReplaceFile(this, courseId, taskId, statusId)（feature-student-timeline.js）
      → 依「這個任務是否走 AI 批改」（taskSupportsAIGrading + hasScript，跟 uploadAudioFilesForGrading 判斷一致）分兩路：
        A. AI 批改：uploadOneAudioBlobWithRetry(..., oldFileId) → submitAudioSegmentsToAIGrading([單一 segment])
           → RPC submit_audio_task_atomic 既有「依 unit_key 合併」邏輯覆蓋該段、其餘 unit_key 原樣保留
        B. 一般檔案：ApiService.uploadToGAS(..., oldFileId) → updateProgress(..., replaceFileId=oldFileId)
           → 既有 student_set_task_completion（raw_data shallow || 合併）整組換掉 submitted_files/drive_file_ids
  → GAS upload_file 新檔成功建立後才 DriveApp.getFileById(oldFileId).setTrashed(true)（by fileId，非檔名比對）
```

### 不變量

```
判斷 AI vs 一般路徑：用 taskSupportsAIGrading()+hasScript（同 uploadAudioFilesForGrading），
  不能只看 unit_key 是否為空——單檔（無分頁）AI 錄音任務 unit_key 本來就是空字串，
  仍必須走 AI 覆蓋路徑，否則替換後不會重新批改
取代 AI 分頁時，original_script／stem／page 必須從 raw_data.audio_segments 取得（依 file_id 對應），
  不能只用 raw_data.submitted_files 的欄位（那裡只有 id/mime/name/unit_key/label，沒有 original_script）
  ——少了 original_script 會把該段的批改文稿洗成空字串
trash 舊檔必須發生在「新檔成功建立之後」，且失敗只記錄、不擋上傳成功的回應
一般檔案取代：updateProgress 的 replaceFileId 只移除該筆再合併，不影響「分批上傳會累加」的既有行為（不傳 replaceFileId 時邏輯完全不變）
AI 分頁取代：沿用既有 submit_audio_task_atomic 依 unit_key 合併語意，不新增 RPC、不新增 webhook 觸發點
  （呼應 ai-grading-pipeline-invariants.mdc：一次 UPDATE 只觸發一次，不可疊加自動重試）
updateProgress 寫表成功後，必須把 rawData 合併回 window._studentTaskCompletions 的對應紀錄再 renderCourses()，
  否則上傳／取代成功後畫面仍是舊快取，音檔播放器／縮圖要等下次整頁重新整理才會出現新檔案
  （2026-08-09 修復：這條之前完全沒做，AI 分頁路徑靠 applyLocalCompletionAfterAudioSubmit 沒事，
  一般／非 AI 路徑則會出現「取代後畫面看起來像沒換」）
guessSubmittedKind 判斷「是不是音檔」只認 mime／檔名副檔名，不分任務類型
  （task.type==='audio_record' 或 'drive' 一般資料夾上傳皆同）；一般資料夾上傳的 <input type="file">
  沒有 accept 限制，副檔名清單要跟其他音檔輸入框的 accept 保持一致（見 2026-08-09 修復，
  補上 .amr/.3gp/.wma/.mp4/.caf/.opus），否則同樣是音檔卻因副檔名沒列到而被當成一般檔案、沒有播放器
已繳交檔案清單的頁碼標籤（labelChip）優先解析 base 範圍文字本身展開出的頁碼（pagesFromRangeText，
  只在展開後的頁數剛好等於檔案數時採用），其次才退回骨架單元列表（grading_units 陣列位置對應）；
  base 範圍文字是老師唯一「看得到、也最準」的頁數來源，不要求老師另外維護一份對得上的骨架列表
  （2026-08-09：光靠骨架單元列表對不上老師實際輸入習慣，導致標籤仍顯示「第 X 檔」）
```

### 禁止

```javascript
// ❌ 用 unit_key 是否有值判斷要不要走 AI 覆蓋（單檔 AI 任務 unit_key 永遠是空字串）
if (unitKey) { /* AI 路徑 */ } else { /* 一般路徑，會漏掉單檔 AI 任務 */ }

// ❌ 取代 AI 分頁時只從 submitted_files 取 original_script（該欄位本來就不在這裡）
const segment = { original_script: meta.original_script }; // undefined，會洗空批改文稿

// ❌ 先 trash 舊檔再上傳新檔（上傳失敗會讓學生連舊檔都不見）
DriveApp.getFileById(oldFileId).setTrashed(true);
var file = folder.createFile(blob);
```

## 改動前自問

若本次會動到 `gas/Code.gs`、`020_js_core/api.js`、學生上傳、或班級資源路徑：是否仍保證「學生繳交直寫 01_Submissions」？若否，停下。

## 改完 Code.gs 後：必須重新部署 GAS（逐步）

Git push **不會**更新線上 Web App。漏部署 = 學生仍跑舊邏輯。

1. 開啟 [Google Apps Script](https://script.google.com/) → 開啟本專案所用的 GAS 專案  
2. 將本機 `gas/Code.gs` **整份貼上／同步**到編輯器（確認含上述 `upload_file` 正確分支）  
3. 按儲存（磁碟圖示／Ctrl/Cmd+S）  
4. 上方 **部署** → **管理部署作業**  
5. 現有 Web App 部署右側 **✎ 編輯**  
6. **版本** 選「新版本」（必要：選舊版本等於沒更新）  
7. 說明可填：`fix: 學生上傳直寫 folderId`  
8. **部署**  
9. 確認網址仍與前端一致：  
   - 學生／共用：`020_js_core/api.js` 的 `GAS_API_URL`  
   - 老師：`110_teacher_core/api-gas-service.js` 的 `GAS_WEB_APP_URL`  
   （若網址變了，前端也要改並 bump `?v=`）  
10. 用學生帳測一次「上傳檔案」→ 檔案應出現在該生 `01_Submissions` 根目錄（不是學生夾下的 `01_Class_Resources`）

## Code review 打勾在哪裡做

- **開 GitHub PR 時**：`.github/PULL_REQUEST_TEMPLATE.md` 會自動帶出 checklist（見該檔）  
- **自己改完未開 PR**：對照上節部署步驟 + 模板裡的 `upload_file` 那一勾，自行確認後再測  
