---
description: AI 批改錯誤做法雷區（曾拖垮整站登入；禁止延遲重試風暴／雙 webhook）
globs: supabase/functions/process-audio-ai/**,110_teacher_core/feature-ai-backfill.js,120_student_core/feature-student-timeline.js
alwaysApply: false
---

# AI 多段批改管線鐵律（雷區｜錯誤做法）

> 2026-07-31：**錯誤的「自動救援」把整站（含登入）打掛。**  
> 改 `process-audio-ai`、補啟、webhook、學生送批前必讀。

## ❌ 絕對禁止的錯誤做法

1. **禁止** `stillPending` 就立刻 `fetch` 自己（空轉風暴）
2. **禁止** 連線失敗／522／timeout 就 `setTimeout` 自動延遲重試再 invoke（會在 DB 不穩時指數放大，拖垮 Auth）
3. **禁止** 多個 concurrent invoke 各搶一段 pending → 6 段全 `processing`、0 `done` 死鎖
4. **禁止** 對卡住的多段案呼叫 `submit_audio_task_atomic` 當「續跑」（會把已 done 全打回 pending，浪費 Speechace）
5. **禁止** 續跑時再疊 `functions.invoke`＋webhook 雙喚醒
6. **禁止** 把 `ai_processing` 從補啟與清除兩邊都排除（老師動不了）
7. **禁止** 清除時預設勾選已批完學生（浪費 API）
8. **禁止** 學生端提供「手動提交／重新提交 AI 批改」按鈕（易誤觸、加重 webhook／Edge 負載；送批只走繳交／上傳成功路徑）
9. **禁止** 在未確認 `task_completions` 上是否有**重複 webhook** 時狂改 Edge  
   - 曾同時存在：`Trigger AI Audio Grading`（INSERT/UPDATE 都打）＋ `ai_processing_webhook`  
   - 緊急時可 `DISABLE TRIGGER`；恢復前必須先確認不會雙觸發風暴
10. **禁止** `submit_audio_task_atomic` 對 `audio_segments`／`drive_file_ids`／`submitted_files` 整批覆寫  
    - 症狀（2026-08-01）：12 頁作業學生先傳 6 個成功，剩下 6 個下次再傳──舊版整批覆寫，
      第一批的 6 段（含可能已完成的 AI 結果）全部消失，且因前端每次都從 `gradingUnits[0]`
      對應，第二批還會被誤標成第 1~6 頁（蓋掉真正的第 1~6 頁）
    - 已修：RPC 依 `unit_key` 合併（新提交蓋過同 key 舊段，其餘舊段保留）；
      前端 `uploadAudioFilesForGrading` 先排除「已提交過」的 unit_key，新選檔案接續對應剩下的頁
    - 見 `.cursor/rules/material-snapshot-refs-invariant.mdc` 之外的另一半：這裡是**送出後**的合併，
      不是套用 Snapshot 時的合併

## ✅ 正確做法

```typescript
// 立刻 continue：僅當本輪有進度且仍有可 claim 的 pending／stale
const shouldContinue = stillPending && claimableAfter >= 0 && segmentsProcessedThisInvoke > 0;

// 同時只允許一段新鮮 processing；其餘 worker 回 -1
// 進場可釋放「過期」processing → pending（勿對新鮮 claim 亂釋放）

// 暫時性錯誤：寫 ai_last_error、釋放本輪 claim，status 保持可恢復；
// 但「不要」自動延遲 re-invoke。改由老師端「續跑」或繳交當下 webhook 單次觸發。
```

- 六段＝**逐段**分批，不是一次送六檔 Speechace  
- 已 `done`＋有 `ai_evaluation` → **跳過**，禁止重打  
- 教師續跑：保留 done，只重跑未完成；可勾選單一學生  
- `ai_processing` 必須在補啟或清除可操作；部分完成要顯示 `x/n`
- 學生分批提交（一頁一檔的多段任務）：新一批只能「補齊」尚未提交的 unit_key，
  不可讓後一批覆蓋前一批；`submit_audio_task_atomic` 與前端對應邏輯都要用 unit_key 判斷，
  不能只靠陣列索引

## 緊急止血（若整站又變慢／無法登入）

1. 部署 `process-audio-ai` **kill-switch**（收到請求立刻 200、不碰 DB）  
2. `DISABLE TRIGGER` 兩個 AI webhook  
3. 把卡住的 `ai_processing` 改回 `submitted`  
4. 網站恢復後再謹慎重開（單 webhook、無延遲自動重試）

完整邏輯備份檔名慣例：`supabase/functions/process-audio-ai/index.ts.bak-stable`

## 檔案

- `supabase/functions/process-audio-ai/index.ts`（改完必 deploy）
- `110_teacher_core/feature-ai-backfill.js`
- 學生端：**不要**再加回手動提交批改 UI（`ui-student-timeline-templates.js`）

## 改動前自問

會不會造成：(a) 自動延遲重試？(b) 無進度 self-continue？(c) 雙 webhook？(d) 學生可手動狂送 AI？  
有任一是 → **停下**。
