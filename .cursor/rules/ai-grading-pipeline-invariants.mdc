---
description: AI 多段批改／補啟鐵律（禁止 continue 空轉風暴、禁止批改中兩邊都動不了）
alwaysApply: true
---

# AI 多段批改管線鐵律（雷區）

## 1. Edge self-continue 空轉風暴（曾拖垮整站含登入）

`process-audio-ai` 若段卡在新鮮 `processing`、本輪 0 段進度，仍立刻 `fetch` 自己 → 空轉打爆 Supabase。

```typescript
// ❌ 禁止：只要 stillPending（含 processing）就 continue
if (stillPending) { fetch(self); }

// ✅ 必須：有可 claim 的 pending／stale，且本輪 segmentsProcessedThisInvoke > 0 才 continue
```

檔案：`supabase/functions/process-audio-ai/index.ts`  
改完必須 `supabase functions deploy process-audio-ai`。

## 2. 六段＝逐段分批，不是一次送六檔

一 invocation 內迴圈批多段（約 50s wall）；已 `done`＋有 `ai_evaluation` 的段**禁止**重打 Speechace。  
續跑卡住：保留 done，只重跑未完成；**禁止**對多段案再呼叫會把全段打回 `pending` 的 RPC 當「續跑」。

## 3. 教師補啟／清除：`ai_processing` 不可兩邊消失

- `ai_processing` 必須出現在「補啟／續跑」**或**「清除」可操作列表（不可兩邊都排除）
- 清除預設只勾批改中／卡住；**禁止**預設連已批完學生一起清（浪費 API）
- 續跑：**不要**再疊 `functions.invoke`＋webhook 雙喚醒（易放大 continue 風暴）

檔案：`110_teacher_core/feature-ai-backfill.js`

## 4. 部分完成要看得見

`ai_processing` 且已有段 `done` 時，學生／教師 UI 應顯示 `已完成 x/n` 與部分報告，不可只顯示「批改中…」黑箱。

## 改動前自問

會不會造成：(a) 無進度仍 self-continue？(b) 批改中列表消失？(c) 已 done 段被重打 Speechace？有任一是，停下。
