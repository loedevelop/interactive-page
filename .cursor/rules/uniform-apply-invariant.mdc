---
description: 規則要全面套用；禁止只修例子、禁止同類列走第二套邏輯
alwaysApply: true
---

# 規則要全面套用

主句：**規則要全面套用。** 少一筆同類就不算做完。

「禁止只修例子」是反面提醒，不是完成條件。多碰一個檔、改一句註解，都不算全面套用。

老師說「像 XX 那樣」＝**全站規格**，不是只修 XX 那一筆。截圖是驗收樣本，不是 if 條件。

## 症狀（禁止只修例子）

- 畫面上看得到的那筆對了（例：Mason／Sean pic／word 兩張卡），別的資料夾沒套同一把鑰匙（例：Jessie 被另寫「活頁名不是單字母就拆卡」）
- 為某一本教材、某一個資料夾名、某一個活頁名長相，新開第二套規則
- 自稱「不是只修例子」，但說不出鑰匙、也沒對過第二筆長相不同的資料

## 不變量

```
先講鑰匙，再套全部同類列。沒有「例子流程」與「其他流程」。
老師點名的資料夾／檔名，只當驗收清單，不准寫進程式當分支條件。
做完必須同時成立：
  1. 鑰匙是什麼（一句話）
  2. 哪些列／資料夾該走同一條路
  3. 至少用兩個不同實例對過（老師點名的那筆 ＋ 另一筆長相不同的）
對不到、會套錯 → 先講，等同意。禁止為了讓例子看起來對而另寫一條。
少一筆 → 未完成。不准用「禁止只修例子」自我開脫。
```

教材區現行鑰匙（未再同意前不准改）：

```
勾了 is_group → 一張卡 = 資料夾 + 擷取範本 + 來源檔（多本活頁共一張，活頁名統整 A～Z）
沒勾 is_group → 一張卡 = 資料夾 + 擷取範本 + 來源檔 + 活頁（一本一張，由上而下）
舊列沒勾過＝false，不准猜成群組
同一張卡可有多本活頁（只有勾了群組才收成一張）；出作業選套餐後，該套餐活頁自動列出，各自選頁。不准再要老師挑活頁
只有擷取範本或來源檔不同，或沒勾的另一本活頁，才多一張卡
活頁名可在同一資料夾重複；不可重複＝資料夾＋活頁名＋擷取範本
群組開關跟活頁列走（material_sheets.is_group）；一次套用寫進這次勾的每一本。勾了＝教材區／改檔名共一顆標籤，只方便套公式。出作業＝一律一本活頁一列，各自選頁；不准因 is_group 把全部活頁綁成一筆。
一個窗口＝一個教材資料夾。窗口頂：教材資料夾，再來源檔（同一來源才共一欄；來源不同就寫在各套餐卡上）。窗口內套餐由上而下依次列出。不准再用「區塊類別」當第二層標題，也不准一個資料夾拆成多個窗口。學生文稿 PDF 掛在資料夾外層（＋來源檔），不准每張套餐卡各傳一份。存檔寫進該組每一個 material_combinations。檔案頁 ≠ 課本頁；對不到就不顯示、不准猜、不准 OCR。上傳在教材範本管理，不在作業
```

改檔名／產出檔名 popup 鑰匙（未再同意前不准改）：

```
一個格式，各處只換資料。
四欄：活頁名＝{活頁}（老師不能手改；短名遺毒由系統依現檔強制改掉）｜活頁別稱＝{別稱}（能改；跟活頁列走，不准只靠活頁名共用）｜meta｜文稿
公式：活頁名 {活頁}（公式格也鎖死）。meta／文稿預設 {活頁}.{範本}（沒有擷取範本才是 {活頁}）。附檔名鎖 .meta.json／.script.txt。下面列先顯示現檔；按「套用公式到全部」才改列。
標籤固定三行：活頁名／擷取範本／試卷範本；沒有的格式名字留白，不准抄、不准寫尚未××
勾了群組＝共一顆標籤，活頁名統整（連續單字母／數字用 A～Z）
沒勾＝一本一顆，不准收成一筆、不准弄丟列
出作業仍一本活頁一列，不准把群組標籤當成一筆範圍
教材區卡片、改活頁／檔名、擷取「產生上傳」同一套三行標籤，不准各寫各的
```

書寫答案／學生文稿鑰匙（未再同意前不准改）：

```
公式只認該擷取範本：書寫＝answer_combine_note；學生文稿＝student_script 每一行（<page title>／<blank>／<data>；沒填才是 _answer_combined_text）
套用只走 MaterialSnapshot.applyExtractionFormulasToRows／formatStudentDisplayBlock／studentLineFromRow
不准把三行學生文稿整份當一個運算式
公式從 FeatureTemplateLibrary.extractionContext(templateId) 讀；禁止各檔自己 find + 自己 evaluate
統計表更新目錄、出作業 Snapshot 同一把。寫進 class_review_catalog_meta 的是算好的結果
沒填＝沒有。禁止拿書寫去補學生文稿、禁止空白接欄當第二套公式
```

## 禁止

```javascript
// ❌ 資料夾名／例子當分支
if (folderName.indexOf('Mason') !== -1) { /* 正確分層 */ }
else { /* Jessie、GEPT 另寫一套 */ }

// ❌ 用活頁名長相當第二套規則
if (!/^[A-Z]$/.test(sheetStem)) splitIntoOwnCard();
```

## 正確

```javascript
// ✅ 全部列同一把 key，再畫卡
const key = folderId + '|' + templateId + '|' + sourceFile + (isGroup ? '' : '|' + sheetStem);
```

## 改動前自問

1. 這把鑰匙是什麼？有沒有寫成「只對截圖那筆成立」的 if？
2. 除了老師點名的例子，另一筆長相不同的資料是否走同一條路？
3. 少一筆同類是否仍敢說做完？（敢 → 停。全面套用才算完成。）
4. 任一否 → 停、提出來、等指示。
