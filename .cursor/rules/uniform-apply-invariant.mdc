---
description: 規則要全面套用；禁止只修例子、禁止同類列走第二套邏輯
alwaysApply: true
---

# 規則要全面套用

主句：**規則要全面套用。** 少一筆同類就不算做完。

「禁止只修例子」是反面提醒，不是完成條件。多碰一個檔、改一句註解，都不算全面套用。

老師說「像 XX 那樣」＝**全站規格**，不是只修 XX 那一筆。截圖是驗收樣本，不是 if 條件。

## 症狀（禁止只修例子）

- 畫面上看得到的那筆對了（例：Mason／Sean pic／word 兩張卡），別的資料夾沒套同一把鑰匙（例：Jessie 被另寫「活頁名不是單字母就拆卡」）
- 為某一本教材、某一個資料夾名、某一個活頁名長相，新開第二套規則
- 自稱「不是只修例子」，但說不出鑰匙、也沒對過第二筆長相不同的資料

## 不變量

```
先講鑰匙，再套全部同類列。沒有「例子流程」與「其他流程」。
老師點名的資料夾／檔名，只當驗收清單，不准寫進程式當分支條件。
做完必須同時成立：
  1. 鑰匙是什麼（一句話）
  2. 哪些列／資料夾該走同一條路
  3. 至少用兩個不同實例對過（老師點名的那筆 ＋ 另一筆長相不同的）
對不到、會套錯 → 先講，等同意。禁止為了讓例子看起來對而另寫一條。
少一筆 → 未完成。不准用「禁止只修例子」自我開脫。
```

教材區現行鑰匙（未再同意前不准改）：

```
勾了 is_group → 一張卡 = 資料夾 + 擷取範本 + 來源檔（多本活頁共一張，活頁名統整 A～Z）
沒勾 is_group → 一張卡 = 資料夾 + 擷取範本 + 來源檔 + 活頁（一本一張，由上而下）
舊列沒勾過＝false，不准猜成群組
同一張卡可有多本活頁（只有勾了群組才收成一張）。沒勾群組＝出作業／出考題選套餐後該套餐活頁自動列出，各自選頁。勾了群組＝區段改下拉，老師自己選這份套餐的哪一本；不准自動列出全部，選項只准這份套餐自己的活頁。
套餐＝活頁 + 擷取範本 + 試卷範本（Excel／JSON 這條，未改）。三個都對到才是這一份。同一本活頁可有多份套餐（試卷範本不同＝不同套餐）。套餐名＝material_combinations.label，只走 comboLabelText。舊句「資料夾＋Excel＋擷取」已作廢，不准再摺疊出作業下拉。改名稱＝改這筆 label，不准新建。
PDF 來源跟 Excel／JSON 同一層，但套餐鑰匙仍是「這份來源＋這份試卷範本」，不是「都是 PDF 就同一類」。PDF 試卷範本兩種：左到右、上到下（預設）；老師定位。試卷範本不同＝不同套餐。群組＝套用時勾選（Excel／JSON 在處理時勾；PDF 在套用試卷範本時勾）。沒勾＝false，不准猜。長篇：docs/組合作業範圍與套餐.md。
教材區 PDF 套用／貼 txt 走獨立模組（feature-material-pdf-exam.js），產生在同一頁獨立區塊，不准寫進程式當 Excel 套用列。不准改 Excel／JSON 套餐程式（ensureCombination／pickComboForCard／combo_statistics 畫卡）。教材區窗口只列已套用的 PDF 卡。
目錄套餐跟 Excel/JSON／PDF 同階層、在教材資料夾、有卡。舊套餐由夾裡的檔產生；目錄由老師出作業提供範圍、系統收集成書。不准拿夾裡 text／PDF 自動產出這張卡。產生在同一頁另一獨立區塊（選夾後＋新增）。獨立模組 feature-material-book.js，不准改 Excel／JSON 畫卡。出作業下拉同一開口 listAssignedCombosForHomework，只列該班已勾。範圍六層主單元／次單元／標題／大題／次題／小題都提供，這次填到哪由老師定。有目錄文稿才寫 txt 進該教材資料夾。目錄文稿與這次錄音口說答案是兩把鑰匙，不准互抄。選目錄不准走 Snapshot／.script.txt 假裝有 meta。
只有擷取範本或來源檔不同，或沒勾的另一本活頁，才多一張卡
活頁名可在同一資料夾重複；不可重複＝資料夾＋活頁名＋擷取範本
群組開關跟活頁列走（material_sheets.is_group）；一次套用寫進這次勾的每一本。勾了＝教材區／改檔名共一顆標籤。出作業下拉也是這一顆；範圍表仍是一次一本活頁一列，各自選頁。不准因 is_group 把全部活頁塞進同一列。
一個窗口＝一個教材資料夾。窗口頂：教材資料夾。窗口內只列出該夾已有套餐（由上而下）。來源檔在每一張套餐卡裡（每個套餐自己一格），不准因「同一來源」或「只有一個套餐」提到窗口外。不准再用「區塊類別」當第二層標題，也不准一個資料夾拆成多個窗口。Drive 教材夾裡有檔＝這個夾是教材（PDF-only 夾不准假裝沒有）。教材範本管理可列夾裡真實檔（meta／script／PDF）供選／上傳；列出檔 ≠ 每個檔各一份套餐。三種產生分三區、不准合併。學生文稿 PDF 掛在資料夾外層，不准每張套餐卡各傳一份、不准拿來當考卷來源。存檔寫進該組每一個 material_combinations。檔案頁 ≠ 課本頁；對不到就不顯示、不准猜、不准 OCR。上傳在教材範本管理，不在作業、不在教材區窗口
```

改檔名／產出檔名 popup 鑰匙（未再同意前不准改）：

```
一個格式，各處只換資料。
四欄：活頁名＝{活頁}（老師不能手改；短名遺毒由系統依現檔強制改掉）｜活頁別稱＝{別稱}（能改；跟活頁列走，不准只靠活頁名共用）｜meta｜文稿
公式：活頁名 {活頁}（公式格也鎖死）。meta／文稿預設 {活頁}.{範本}（沒有擷取範本才是 {活頁}）。附檔名鎖 .meta.json／.script.txt。下面列先顯示現檔；按「套用公式到全部」才改列。
改檔名下面列＝這份套餐自己的雲端現檔。來源是試算表＝{活頁}.meta.json（短檔，這份產出的實況）。來源已是 json＝只認 {活頁}.{這份擷取範本}.meta.json，短檔不是這份的。不是拿公式去對拍。出題／抽檔同一把鑰匙。儲存改名只改這份自己的檔。禁止剛好一筆就抓別的活頁。
標籤固定三行：活頁名／擷取範本／試卷範本；沒有的格式名字留白，不准抄、不准寫尚未××
勾了群組＝共一顆標籤，活頁名統整。連續的活頁字母／數字收成 A～Z（A.sentence-translation 的活頁字母是 A，一樣收）。收不收看勾，不准用「整個名字是不是單字母」當第二套條件。
沒勾＝一本一顆，不准收成一筆、不准弄丟列
勾了群組＝教材區／改檔名共一顆標籤、套公式；出作業／出考題區段下拉選這份套餐自己的活頁，一次選一本、各自選頁。不是結合，不准把隔壁套餐的檔併進來。沒勾＝一本一列自動列出。
教材區卡片、改活頁／檔名、擷取「產生上傳」同一套三行標籤，不准各寫各的
```

書寫答案／學生文稿鑰匙（未再同意前不准改）：

```
公式只認該擷取範本：書寫＝answer_combine_note；學生文稿＝student_script 每一行（<page title>／<blank>／<data>；沒填才是 _answer_combined_text）
套用只走 MaterialSnapshot.applyExtractionFormulasToRows／formatStudentDisplayBlock／studentLineFromRow
不准把三行學生文稿整份當一個運算式
公式從 FeatureTemplateLibrary.extractionContext(templateId) 讀；禁止各檔自己 find + 自己 evaluate
統計表更新目錄、出作業 Snapshot 同一把。寫進 class_review_catalog_meta 的是算好的結果
沒填＝沒有。禁止拿書寫去補學生文稿、禁止空白接欄當第二套公式
```

## 禁止

```javascript
// ❌ 資料夾名／例子當分支
if (folderName.indexOf('Mason') !== -1) { /* 正確分層 */ }
else { /* Jessie、GEPT 另寫一套 */ }

// ❌ 用活頁名長相當第二套規則
if (!/^[A-Z]$/.test(sheetStem)) splitIntoOwnCard();
```

## 正確

```javascript
// ✅ 全部列同一把 key，再畫卡
const key = folderId + '|' + templateId + '|' + sourceFile + (isGroup ? '' : '|' + sheetStem);
```

## 改動前自問

1. 這把鑰匙是什麼？有沒有寫成「只對截圖那筆成立」的 if？
2. 除了老師點名的例子，另一筆長相不同的資料是否走同一條路？
3. 少一筆同類是否仍敢說做完？（敢 → 停。全面套用才算完成。）
4. 任一否 → 停、提出來、等指示。
