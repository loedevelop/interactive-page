---
description: 永遠不准丟資料；對不到格式就略過／從畫面拿掉／當沒有
alwaysApply: true
---

# 永遠不准丟資料（死律）

**丟資料＝極度不專業。** 資料已經在手上，就必須留下。不知怎麼處理 ≠ 可以丟掉。對不到格式 ≠ 沒有這筆。不准為了讓程式跑得過、畫面看起來整齊，就把「你不會處理的」刪掉、略過、從清單拿掉。

這條與精準（`precision-no-borrow-invariant.mdc`）同時成立，不衝突：

- 精準：去**對某一把鑰匙**（完整檔名／id）。對不到＝沒有那一把，不准改借隔壁。
- 本條：資料**已經在輸入裡**（老師貼的原文、掃到的列、畫面上的組）。有字／有列／有組＝存在，必須留下。

空行沒有資料，可以跳過。除此之外，不准默默捨棄。

## 症狀

- 解答原文第一行是 `Quiz 1, p. 1`，下面是 `walk, are walking`，清單卻從 Quiz 2 開始；「改用老師定位」沒有 Quiz 1
- 某一組沒解析到，警示、按鈕、預覽就把這一組當不存在
- 標頭同一行後面還有答案、題號前面還有字、`A.` 後面還有正文，解析後消失
- 編號 6 然後 8，Quiz 7 從畫面消失

## 根因

`020_js_core/pdf-exam-paper.js` 的 `parseAnswerText` 曾只認 `N.` 題號行。沒有題號的答案行（Quiz 1）走完迴圈沒有 `last` 就什麼都不做。UI 只畫 `parsed_bank` 裡出現過的組 → 原文有的組從畫面消失。這是消去法：不會處理就當沒有。

## 不變量

```
有資料就留下。老師貼的每一行有字的答案、掃到的每一組、編號 1…N 裡的每一號，都還在系統裡。
對不到你熟悉的格式 → 收進這一組（依出現順序當下一題）、紅字警示、給老師核對或改用老師定位。不准刪。
有大題標頭＝這一組存在。沒有「1.」題號不准把該組整段丟掉。
標頭同一行後面的字、題號前面的字、A. 後面的正文＝答案，一律收進清單。
編號中間缺號＝缺的那一組仍要列出，不准從畫面拿掉。
空行＝沒有資料，可以跳過。其餘不准默默 return／filter 掉。
```

## 禁止

```javascript
// ❌ 對不到題號就整行丟掉
if (!matches.length) { /* 沒 last 就什麼都不做 */ return; }

// ❌ 不會處理的組從畫面拿掉
groupItemsBySection(bank); // 只畫解析到的組，Quiz 1 原文有也不出現

if (!paperLabels.includes('Quiz 1')) { /* 當 Quiz 1 不存在 */ }
```

## 正確

```javascript
// ✅ 沒有「1.」＝這一組依出現順序的下一題。有字就收。
ingestUnnumbered(line);

// ✅ 缺號的組仍列出，不准拿掉
groupItemsBySectionWithMissing(bank, review.missing_sections);
```

## 實測

1. 原文以 `Quiz 1, p. 1` 開頭，下面沒有 `1.`，只有 `walk, are walking` → 解析後清單最上面是 Quiz 1，這些行都在。
2. 警示與「改用老師定位」有 Quiz 1（以及編號缺口的缺組），不是從 Quiz 2 起跳。
3. 有 `1.` 的大題（問句換行、OR）仍在，沒有為了收沒題號的行就把別組吃掉。

## 改動前自問

1. 這筆資料是老師已經給的（原文／掃到的列／畫面上的組），還是根本不存在的鑰匙？
2. 對不到格式時，程式會不會 `return`、略過、從清單拿掉、當成沒有？
任一「會」→ 停。留下資料，警示，等老師處理。
