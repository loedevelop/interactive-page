---
description: 教材 Snapshot／節點 meta 列鐵律（反覆出現：存檔後只剩 A、第二冊不進 snap、畫面 0 列）
globs: 110_teacher_core/store-assignment-builder.js,110_teacher_core/feature-timeline.js,110_teacher_core/ui-timeline-templates.js
alwaysApply: false
---

# 教材 Snapshot／節點 meta 鐵律（雷區｜反覆出錯）

> **這是反覆出現的老問題。** 標題／凍結 snapshot 顯示 A～F，下方逐頁批改稿還在，但 meta 列變成只剩 A、甚至 0 列、base 範圍被清空；或第二冊 meta 選了卻不進預覽／snap。  
> **與音檔切割無關。** 每次改作業編輯器／Snapshot／存檔，必讀本檔。

## 症狀（老師端）

- 標題：`A pp. 1~2；…；F pp. 1~2`（來自 base 範圍）
- 凍結 snapshot 字樣仍寫 A～F（或只寫到 A）
- `grading_units` 仍有多頁／或只剩 A
- **meta 列只剩 `A.meta.json` 或完全空白**；base 範圍 input 空掉
- **第二冊**：base 範圍已是 `A…；B…`，凍結卻只有 `A pp.…`，逐頁只有 A（2026-08-01）

## 根因（不可再犯）

1. 存檔用 **DOM meta 列**（hydrate 未完成／只渲染到 1 列）**覆寫**較完整的 `material_refs`
2. hidden snapshot JSON **只存單一** `material_ref` → 存檔砍成 `[material_ref]`
3. 「依列重算」用殘缺列把 `material_range`／標題縮成只剩 A
4. **hydrate 時 meta 清單 API 失敗**，catch 只改狀態文字、**不 `renderMaterialMetaRows`** → 畫面 0 列（2026-07-31 再犯）
5. **自動 Snapshot busy 時直接 `return`**：第一冊 GAS 還在跑，第二冊填完被丟掉，結束後也不重跑（2026-08-01）
6. **`applySnapshotToNode` 的 async `ensureMetaCatalog.then(renderMaterialMetaRows)`**：A 套用後老師立刻 +B，舊 callback 用只有 A 的 `material_refs` **蓋掉 B 列**
7. **`BuilderStore.sync` 不合併 `meta_rows_by_stem`**，或用 DOM 殘缺 `grading_units` 縮水冊數 → 考試右欄 B 永遠算不到真可用題

## 不變量

```
unique(stems in grading_units) === length(material_refs)
凍結後 grading_units = AI／一頁一檔主真相
禁止用較短 DOM／單一 material_ref 覆寫較長 material_refs
hydrate：無論清單 API 成敗，都必須先畫出 meta 列
自動套用：busy 期間變更必須 pending，結束後重跑
async 重繪 rows：世代號 + DOM 冊數 > snapshot 冊數時禁止縮水
meta_rows_by_stem：sync 時 merge，禁止用較舊單冊 snap 蓋掉已累積冊
```

## 禁止

```javascript
// ❌ DOM 有幾列就寫幾列
t.raw_data.material_refs = rowsFromDom;

// ❌ 舊 snapshot 只有 material_ref 就砍成一筆
t.raw_data.material_refs = [snap.material_ref];

// ❌ 清單載入失敗就不 render（畫面變 0 列）
loadMeta().catch(err => { statusEl.textContent = err.message; });

// ❌ busy 直接丢掉第二次自動套用
if (_autoSnapshotBusy[path]) return;

// ❌ 不檢查世代就把 snapshot.material_refs 重繪進 DOM
ensureMetaCatalog().then(opts => renderMaterialMetaRows(path, opts, snapRowsOnlyA));
```

## 正確做法

```javascript
// ✅ ensureMaterialRefsMatchUnits(refs, grading_units, templateRef)
// ✅ 先 sync renderMaterialMetaRows(...)，再非同步載入檔名清單
// ✅ 清單失敗仍保留已畫的列（fallback option）
// ✅ snapshot JSON 含 material_refs[] + material_range + meta_rows_by_stem
// ✅ busy → pending；finally 若 pending 再 scheduleAutoSnapshot
// ✅ _snapshotApplyGen + liveRows.length > snapRows.length 則跳過重繪
// ✅ sync：meta_rows_by_stem = Object.assign({}, prev, snap.meta_rows_by_stem)
```

## 改動前自問

會動到 `store-assignment-builder.js`／`feature-timeline.js`／`ui-timeline-templates.js` 時：  
1. 多冊 Snapshot 存檔→重開後，meta 列數是否仍等於 `grading_units` 的 stem 數？  
2. 先套 A 再立刻 +B，凍結字樣與逐頁是否含 B？  
若任一否，停下。

## 實測

- 套用 A～F → 存 → 重開 → 六列仍在、base 範圍完整  
- 斷網／清單 API 失敗 → 仍看得到已還原的 meta 列（不是空白）  
- 選 A＋範圍 → 自動套用中立刻 +B＋範圍 → 結束後凍結含 `A…；B…`，逐頁有 B  
- 考試區段 A 可用題為綠字實數、B 在未進 snap 前顯示「需Snapshot」（不是 `~20` 裝沒事）
